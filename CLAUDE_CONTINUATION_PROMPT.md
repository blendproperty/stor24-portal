# Claude continuation prompt — STOR 24

Continue the STOR 24 programme from the current canonical repository evidence, not from prior chat history.

Before making any change:

1. Read the complete current `PROJECT_CONTEXT.md` in `blendproperty/stor24-portal` (`main`), `blendproperty/stor24` (`master`), and whichever of `blendproperty/stor24-cms` or `blendproperty/blendsign` the selected slice touches.
2. Fetch each relevant remote and report the exact repository, checkout path, branch, `HEAD`, upstream head and working-tree status. Preserve unrelated local changes. Do not use stale Sitelink worktrees as promotion sources.
3. Confirm the system boundary: CRM owns customers, leads, units, reservations, occupancies, operational communication evidence and finance orchestration; the public site owns presentation/orchestration; CMS owns editorial content; BlendSign owns envelopes, signatures, sealed documents and certificates; MRI owns finance once the integration design is approved.
4. Treat health checks, builds and pushes only as their actual evidence level. They do not prove deployment, configuration, provider delivery, money movement, access-control action, business UAT or production correctness.

Choose the highest-priority unblocked slice from the handover snapshot. Start with the human-controlled public booking and pending-account readback only if Brett explicitly approves the state-changing UAT and supplies a consenting test recipient. Otherwise proceed with a safe non-provider slice such as preparing the two-device offline UAT, public-site residual visual QA, monitoring diagnosis, migration rehearsal tooling against non-sensitive sample data, or CMS MFA implementation in its own repository.

Do not implement the unit-transfer signing document until Legal approves the instrument and rules. Keep general WhatsApp lifecycle automation disabled until controlled callback/opt-out/failure UAT passes. Keep Pay Now labelled and gated as a simulator; do not enable Netcash/debit-order flows without verified contracts, credentials, authenticated idempotent callbacks, sandbox UAT and reconciliation. Do not invent MRI mappings, insurance values, Hikvision credentials or POPIA decisions.

For the selected slice, provide a short evidence plan, implement only the approved scope, run proportionate validation, inspect the diff, and then clearly report: implementation; testing; commit and push; merge; deployment/configuration; live production verification; UAT/approval; remaining blockers. Update every affected canonical `PROJECT_CONTEXT.md` with dated evidence and reconcile Asana if it is in scope. Confirm the context update is present on the intended remote production branch before describing the slice as complete.
